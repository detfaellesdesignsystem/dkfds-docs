{% from "components/buttons/buttons.njk" import button as button %}

<!-- Placering: top -->
{{ 
    button({
        "variant": "primary",
        "text": "Top",
        "tooltip": {
            "text": "Top tooltip",
            "position": "top"
        }
    }) 
}}

<!-- Placering: bund -->
{{ 
    button({
        "variant": "primary",
        "text": "Bund",
        "tooltip": {
            "text": "Bund tooltip",
            "position": "bottom"
        }
    }) 
}}
