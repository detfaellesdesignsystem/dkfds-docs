{% from "components/buttons/buttons.njk" import button as button %}

{{ 
    button({
        "variant": "primary",
        "text": "Knap med tooltip",
        "tooltip": {
            "text": "Dette er et tooltip"
        }
    }) 
}}
