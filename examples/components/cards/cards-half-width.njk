{% from "components/cards/cards.njk" import card as card %}

<div class="container pb-5">
  <div class="row">
    <div class="col-12 col-sm-12 col-md-6 col-lg-6">
      {% for item in cards2_example.items -%}
        {{ card(item) }}
      {%- endfor %}
    </div>
    <div class="col-12 col-sm-12 col-md-6 col-lg-6 mt-6 mt-md-0">
      {% for item in cards3_example.items -%}
        {{ card(item) }}
      {%- endfor %}
    </div>
  </div>
</div>
