{% from "components/cards/cards.njk" import card as card %}

<div class="container pb-5">
  <div class="row">
    <div class="col-12 col-sm-12 col-md-4 col-lg-4">
      {% for item in cards4_example.items -%}
        {{ card(item) }}
      {%- endfor %}
    </div>
    <div class="col-12 col-sm-12 col-md-4 col-lg-4 mt-6 mt-md-0">
      {% for item in cards5_example.items -%}
        {{ card(item) }}
      {%- endfor %}
    </div>
    <div class="col-12 col-sm-12 col-md-4 col-lg-4 mt-6 mt-md-0">
      {% for item in cards6_example.items -%}
        {{ card(item) }}
      {%- endfor %}
    </div>
  </div>
</div>
