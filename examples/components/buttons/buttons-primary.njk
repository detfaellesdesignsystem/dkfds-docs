{% from "components/buttons/buttons.njk" import button as button %}

<div class="container py-8">
{% for item in button_example.items %}
  {{ button(item.text, item.class, item.attribute, item.icon, item.sronly) }}
{% endfor %}
</div>

