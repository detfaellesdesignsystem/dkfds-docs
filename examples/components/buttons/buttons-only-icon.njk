{% from "components/buttons/buttons.njk" import button as button %}

{{ 
    button({
        "variant": "unstyled",
        "icon": "trash-can",
        "srOnly": "Slet",
        "classes": { "icon": "mr-0 mt-0" }
    }) 
}}
