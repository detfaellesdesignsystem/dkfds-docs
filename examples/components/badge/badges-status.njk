{% from "components/badge/badges.njk" import badge as badge %}

<div class="container pb-5">
  {%- for badges in badge_example_small.items -%}
    <p class="mb-5">{{ badges.title }}<br />
    {{ badge(badges.class, badges.label) }}
    </p>
  {% endfor -%}
</div>


