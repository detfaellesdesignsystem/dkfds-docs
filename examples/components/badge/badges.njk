{% from "components/badge/badges.njk" import badge as badge %}

<div class="container pb-5">
  {%- for badges in badge_example.items -%}
    {%- if badges.heading %}
      <p class="h5">{{ badges.heading }}</p>
    {% endif -%}
    {{ badge(badges.class, badges.label) }}
  {% endfor -%}
</div>


