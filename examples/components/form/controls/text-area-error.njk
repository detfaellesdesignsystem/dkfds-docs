{% from "components/form/controls/text-area.njk" import textarea as textarea %}

{{textarea(item.id, item.label, item.optionallabel, item.class, item.name, item.classLabel, item.placeholder, item.hint, item.error, item.rows)}}
